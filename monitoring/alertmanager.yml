global:
  resolve_timeout: 5m
route:
  receiver: 'email-alert'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: 'email-alert'
    email_configs:
      - to: 'alerts@rezapp.com'
        from: 'monitoring@rezapp.com'
        smarthost: 'smtp.gmail.com:587'
