port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 60000
  tcp_port: 7881
  udp_port: 7882
  node_ip: ${NODE_IP:-}
  use_ice_lite: true
  use_external_ip: false
keys:
  devkey: secret
logging:
  level: info
room:
  empty_timeout: 300
  max_participants: 10
# SIP trunk konfigürasyonu LiveKit API üzerinden yapılır (panelden yönetilir)
